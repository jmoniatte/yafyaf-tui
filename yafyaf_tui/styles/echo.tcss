/* ═══════════════════════════════════════════════════════════════════════
   ECHO
   ═══════════════════════════════════════════════════════════════════════ */

/* In flow under the list rather than docked, so it sits above the status line when that shows */
#echo {
    height: 2;
    padding: 0 1;
    border-top: solid $gutter;
    color: $comment;
    text-style: italic;
    text-wrap: nowrap;
    text-overflow: ellipsis;
}

/* The score sits in the header, left of the account */
#echo-score {
    width: auto;
    margin-left: 2;
    color: $green;
}
